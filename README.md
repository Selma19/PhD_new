The goal of this branch is:
- to fill the stimulus db
- to check that the filled data are correct

This code is supposed to run on the gwdg hpc.

This code is submitted as batch files gathered in the directory slurm_scripts.
Each script will launch a different python script, numbered with the same integer.
Documentation about these numberings can be found in the same directory as the batch files as .md files.

